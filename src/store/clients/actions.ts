import { ActionTree } from 'vuex';
import { ClientsState, ClientSelectRequest } from './types';
import { RootState } from '../types';

export const actions: ActionTree<ClientsState, RootState> = {
};
